FROM nginx:latest

RUN rm -f /etc/nginx/conf.d/default.conf
COPY config/nginx/phpmyadmin.conf /etc/nginx/conf.d/
COPY config/nginx/elasticsearch.conf /etc/nginx/conf.d/

RUN mkdir /etc/nginx/ssl && chmod -R 600 /etc/nginx/ssl

COPY config/nginx/ssl/* /etc/nginx/ssl/
